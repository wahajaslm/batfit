--extra-index-url https://download.pytorch.org/whl/cu126

# Core training stack (Torch + HF ecosystem).
torch==2.8.0+cu126 ; platform_system == "Linux" and platform_machine == "x86_64"
torch==2.8.0 ; platform_system != "Linux" or platform_machine != "x86_64"
torchvision==0.23.0+cu126 ; platform_system == "Linux" and platform_machine == "x86_64"
torchaudio==2.8.0+cu126 ; platform_system == "Linux" and platform_machine == "x86_64"
accelerate==1.10.1
transformers==4.46.3
peft==0.17.1
datasets==4.4.1
sentencepiece
scipy
pyyaml
jsonschema==4.23.0
pyarrow==21.0.0

# Optional GPU optimizations. bitsandbytes currently lacks Py3.12 wheels, so guard it.
bitsandbytes==0.44.1 ; python_version < "3.12" and platform_system == "Linux" and platform_machine == "x86_64"
triton==3.4.0 ; platform_system == "Linux" and platform_machine == "x86_64"
